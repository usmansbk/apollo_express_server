type User implements Person {
  id: ID!
  firstName: String!
  lastName: String!
  email: String!
  roles: [Role!]
  verified: Boolean
  phoneNumber: String
  password: String
  updatedAt: Date!
  createdAt: Date!
}

input UpdateUserInput {
  id: ID!
  firstName: String
  lastName: String
  phoneNumber: String
}

input SearchUserFilterInput {
  id: ID
  firstName: String
  lastName: String
  size: Int
  after: ID
  sort: Order
}
