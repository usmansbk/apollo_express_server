type User {
  id: ID!
  firstName: String!
  lastName: String!
  email: String!
  verified: Boolean
  phoneNumber: String
  updatedAt: Date!
  createdAt: Date!
}

input UpdateUserInput {
  id: ID!
  firstName: String
  lastName: String
  phoneNumber: String
}

input SearchUserFilterInput {
  id: ID
  firstName: String
  lastName: String
  size: Int
  after: ID
  sort: Order
}
