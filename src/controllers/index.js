import Auth from './auth';
import SocialAuth from './social_auth';
import User from './user';
import Admin from './admin';

export {
  Auth, User, SocialAuth, Admin,
};

export default {
  Auth,
  User,
  SocialAuth,
  Admin,
};
